import SwiftUI

/// Web `globals.css` `--primary` (#ED4531) ile hizalı.
enum PromptlyBrand {
    static let primary = Color(red: 237 / 255, green: 69 / 255, blue: 49 / 255)
    static let primaryLight = Color(red: 1.0, green: 0.45, blue: 0.38)
    static let primaryDark = Color(red: 200 / 255, green: 55 / 255, blue: 38 / 255)
}
